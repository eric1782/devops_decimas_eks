apiVersion: v1
kind: Service
metadata:
  name: decimas-db
  namespace: decimas
spec:
  selector:
    app: decimas-db
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  clusterIP: None
