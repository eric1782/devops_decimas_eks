apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: decimas-backend-hpa
  namespace: decimas
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: decimas-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
